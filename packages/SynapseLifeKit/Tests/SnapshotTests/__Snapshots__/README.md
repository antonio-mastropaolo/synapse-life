# Snapshot references

Generated once on M2 with `swift-snapshot-testing` and committed alongside the
test source. Re-record by deleting the relevant `.png` and re-running the
suite; intentional UI changes should land in the same commit as a refreshed
reference.

Layout: `SpotlightScreenSnapshotTests/<test-name>.1.png`.
